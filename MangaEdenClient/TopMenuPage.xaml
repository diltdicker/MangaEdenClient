<Page
    x:Class="MangaEdenClient.TopMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MangaEdenClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dao="using:MangaEdenClient.DAO"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GridView Grid.Column="0" Grid.Row="1" ItemsSource="{x:Bind observableManga}">
            <GridView.ItemTemplate>
                <DataTemplate x:Name="MangaTemplate" x:DataType="dao:Manga">
                    <StackPanel Orientation="Vertical" Margin="20" Width="200">
                        <TextBlock Text="{x:Bind Title}"/>
                        <Image Source="{x:Bind ImageString}"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.Items>
                
            </GridView.Items>
        </GridView>
    </Grid>
</Page>
